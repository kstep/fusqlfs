language: perl
perl:
    - "5.16"
    - "5.14"
    - "5.12"
    - "5.10"
before_install:
    - sudo apt-get install -qq libfuse-dev libdbd-sqlite3-perl libdbd-pg-perl libdbd-mysql-perl
after_failure:
    - cat /home/travis/.cpanm/build.log
before_script: "make buildtests"
script: "make test"
